server:
  port: 8081

spring:
  datasource:
    url: ${DB_URL} jdbc:mysql://localhost:3307/nomorework
    username: ${DB_USERNAME} root
    password: ${DB_PASSWORD} root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_NAME} "521366344588-sfn632uud8t5t09sn8aq9civalt901b6.apps.googleusercontent.com"
            client-secret: ${GOOGLE_CLIENT_SECRET} "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"
            scope:
              - email
              - profile


#flyway:
#  enabled: true
#  locations: classpath:db/migration


